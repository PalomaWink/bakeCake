// Exemplo genérico de um modelo de cliente
class Cliente {
  constructor(nome, email, telefone) {
    this.nome = nome;
    this.email = email;
    this.telefone = telefone;
  }
  
  // Métodos para interagir com o banco de dados (ex: salvar, atualizar, etc.)
}

module.exports = Cliente;
